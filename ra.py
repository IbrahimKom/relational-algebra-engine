#!/usr/bin/env python3
"""Entry point -- see README.md. Kept as a thin wrapper so `python ra.py ...`
works from the repo root exactly as the assignment's example invokes it."""

from src.cli import main

if __name__ == "__main__":
    main()
